{
    "scripts": {
        "dev": "wrangler dev --remote",
        "deploy": "wrangler deploy --minify",
        "generate": "drizzle-kit generate:sqlite --out ./db/migrations --breakpoints --schema=./db/schema.ts"
    },
    "dependencies": {
        "@hono/zod-validator": "^0.1.11",
        "@libsql/client": "^0.3.6",
        "@tsndr/cloudflare-worker-jwt": "^2.2.10",
        "drizzle-kit": "^0.20.2",
        "drizzle-orm": "^0.29.0",
        "hono": "^3.10.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20230914.0",
        "wrangler": "^3.14.0"
    }
}
